# Use legacy peer deps to handle dependency conflicts
legacy-peer-deps=true

# Engine strict to ensure Node.js version compatibility
engine-strict=true

# Save exact versions for better reproducibility
save-exact=true

# Disable package-lock for faster CI builds (optional)
# package-lock=false
